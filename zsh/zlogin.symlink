if command -v fastfetch &> /dev/null
then
    fastfetch
else
    echo "------------------------------------------------------------"
    echo "👋 Hello ${(C)USER}!"
    display-system-info
    echo "------------------------------------------------------------"
fi

if command -v brew-upgrade-reminder &> /dev/null
then
    brew-upgrade-reminder
fi

cd $(get-startdirectory)
