if command -v fastfetch &> /dev/null
then
    fastfetch
else
    echo "------------------------------------------------------------"
    echo "👋 Hello ${(C)USER}!"
    display-system-info
    echo "------------------------------------------------------------"
fi
cd $(get-startdirectory)
