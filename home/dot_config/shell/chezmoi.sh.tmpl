#!/bin/bash
# Chezmoi configuration variables
# This file is auto-generated from chezmoi template

# {{ if .githubUsername }}
# Set GitHub username from chezmoi config for use in scripts
export CHEZMOI_GITHUB_USERNAME="{{ .githubUsername }}"
# {{ end }}

# Windows Enterprise Environment Detection (works on Windows and WSL)
export CHEZMOI_IS_ENTRA_ID_JOINED="{{ .isEntraIDJoined }}"
export CHEZMOI_IS_INTUNE_JOINED="{{ .isIntuneJoined }}"
export CHEZMOI_IS_ENTRA_REGISTERED="{{ .isEntraRegistered }}"
export CHEZMOI_IS_AD_DOMAIN_JOINED="{{ .isADDomainJoined }}"
# {{ if .entraIDTenantName }}
export CHEZMOI_ENTRA_ID_TENANT_NAME="{{ .entraIDTenantName }}"
# {{ end }}
# {{ if .entraIDTenantId }}
export CHEZMOI_ENTRA_ID_TENANT_ID="{{ .entraIDTenantId }}"
# {{ end }}
export CHEZMOI_IS_WORK="{{ .isWork }}"
