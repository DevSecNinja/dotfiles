name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  validate:
    name: Validate Dotfiles
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run pre-commit checks
        run: pre-commit run --all-files

      - name: Validate Chezmoi configuration
        run: ./scripts/validate-chezmoi.sh

      - name: Validate shell scripts syntax
        run: ./scripts/validate-shell-scripts.sh

      - name: Validate Fish configuration
        run: ./scripts/validate-fish-config.sh

      - name: Test Chezmoi apply (dry-run)
        run: ./scripts/test-chezmoi-apply.sh

  test-install:
    name: Test Installation
    runs-on: ubuntu-latest
    needs: validate
    container:
      image: mcr.microsoft.com/devcontainers/base:debian-12
      env:
        CODESPACES: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Run install script
        run: ./install.sh

      - name: Verify Chezmoi installation
        run: |
          if ! command -v "$HOME/.local/bin/chezmoi" >/dev/null 2>&1; then
            echo "❌ Chezmoi not found"
            exit 1
          fi
          echo "✅ Chezmoi installed successfully"

      - name: Verify dotfiles were applied
        run: ./scripts/verify-dotfiles.sh

      - name: Run chezmoi verify
        run: |
          "$HOME/.local/bin/chezmoi" verify
          echo "✅ All managed files verified successfully"

      - name: Display chezmoi status
        run: |
          echo "📊 Chezmoi status:"
          "$HOME/.local/bin/chezmoi" managed
          echo ""
          echo "📋 Chezmoi data:"
          "$HOME/.local/bin/chezmoi" data

      - name: Test Fish configuration
        run: ./scripts/test-fish-config.sh

  test-light-server:
    name: Test Light Server Installation
    runs-on: ubuntu-latest
    needs: [validate, test-install]
    container:
      image: mcr.microsoft.com/devcontainers/base:debian-12
      options: --hostname SVLPROD01
      env:
        CODESPACES: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Test light server installation
        run: ./.github/scripts/test-light-server.sh

  test-dev-server:
    name: Test Dev Server Installation
    runs-on: ubuntu-latest
    needs: [validate, test-install]
    container:
      image: mcr.microsoft.com/devcontainers/base:debian-12
      options: --hostname SVLDEV01
      env:
        CODESPACES: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Test dev server installation
        run: ./.github/scripts/test-dev-server.sh

  test-windows:
    name: Test Windows Installation
    runs-on: windows-latest
    needs: validate

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Run Windows install script
        shell: pwsh
        run: |
          .\install.ps1
          if ($LASTEXITCODE -ne 0) {
            Write-Error "Install script failed"
            exit 1
          }

          # Find and add chezmoi to PATH for subsequent steps
          $chezmoiExe = Get-Command chezmoi -ErrorAction SilentlyContinue
          if ($chezmoiExe) {
            $chezmoiDir = Split-Path -Parent $chezmoiExe.Source
            echo "$chezmoiDir" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
            Write-Host "✅ Added chezmoi to GitHub Actions PATH: $chezmoiDir"
          } else {
            Write-Warning "⚠️ Chezmoi not found in current session"
          }

      - name: Verify Chezmoi installation
        shell: pwsh
        run: |
          $chezmoi = Get-Command chezmoi -ErrorAction SilentlyContinue
          if (-not $chezmoi) {
            Write-Error "❌ Chezmoi not found in PATH"
            exit 1
          }
          Write-Host "✅ Chezmoi installed successfully at $($chezmoi.Source)"

      - name: Verify PowerShell profile
        shell: pwsh
        run: |
          $profilePath = "$env:USERPROFILE\.config\powershell\profile.ps1"
          if (-not (Test-Path $profilePath)) {
            Write-Error "❌ PowerShell profile not found"
            exit 1
          }
          Write-Host "✅ PowerShell profile applied"

      # - name: Verify PowerShell profile symbolic link
      #   shell: pwsh
      #   run: |
      #     $profileSource = "$env:USERPROFILE\.config\powershell\profile.ps1"
      #     $profileTarget = $PROFILE

      #     if (-not (Test-Path $profileTarget)) {
      #       Write-Error "❌ PowerShell profile link not created at $profileTarget"
      #       exit 1
      #     }

      #     $item = Get-Item $profileTarget
      #     if ($item.LinkType -ne "SymbolicLink") {
      #       Write-Error "❌ Profile at $profileTarget is not a symbolic link"
      #       exit 1
      #     }

      #     if ($item.Target -ne $profileSource) {
      #       Write-Error "❌ Profile symbolic link points to $($item.Target) instead of $profileSource"
      #       exit 1
      #     }

      #     Write-Host "✅ PowerShell profile correctly linked: $profileTarget -> $profileSource"

      - name: Display Chezmoi status
        shell: pwsh
        run: |
          Write-Host "📊 Chezmoi managed files:"
          chezmoi managed
          Write-Host ""
          Write-Host "📋 Chezmoi data:"
          chezmoi data

      - name: Verify Git configuration
        shell: pwsh
        run: |
          $gitConfigPath = "$env:USERPROFILE\.config\git\config"
          if (-not (Test-Path $gitConfigPath)) {
            Write-Error "❌ Git config not found"
            exit 1
          }
          Write-Host "✅ Git configuration applied"
