# Find a string in files from a directory
Find-String () {
    grep -R $1 $2
}

# Compares two binary files and displays them with color differences
# https://superuser.com/a/968863
Compare-Binary() {
     colordiff -y <(xxd $1) <(xxd $2)
}
